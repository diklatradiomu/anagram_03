{
  "name": "game-anagram-vite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-5000}",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-5000}"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
